export function firstOrNextMessage():string {
    return 'the testAgent.md are empty, theres nothing there'
}


// I need you to create an agent to write unitary and end2end tests for all the system features. as example, now we have the llm connection, the chroma db connection, and csv parser. you must to be pragmatic, do not write unnecessary tests. theres already a paste called agent and a file called testAgent that you should use to write the agent

// agent paste have only a testAgent.md file that you should use to create the agent. the language/framework for this project is nodejs. i need a scrpit/tool that automatically generates test cases when i call (im going to add a command for this agent later). about the constraints or scope, can you run some command to verify the project?